# MSPOLSLPlanZajec / Front-end

----
## install dependencies
    npm install live-server -g
    npm install webpack -g
    npm install 
    bower install

## start server
    npm start

## run docker
    docker build -t schedule . && docker run -p 8080:8080 --name schedule schedule